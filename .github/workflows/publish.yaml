name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@69f3a726de9669df28e5141637b2994484c83f43
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      test-goals: 'run lint'
      node-version: '21'
      slack-channel: '#frontendstøtte-ci'
      slack-bot-name: 'Biome'
      slack-bot-icon: ':mag:'

