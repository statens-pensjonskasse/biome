name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@713f597819c8746d7105376eecf3c218d56a4794
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      node-version: '21'
      slack-channel: '#github-alerts'
      slack-bot-name: 'GitHub Actions'
      slack-bot-icon: ':monkey:'

