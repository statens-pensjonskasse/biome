name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@1ca26341b18424f448c8a849d4f6bf6b9ccb29d6
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      test-goals: 'run lint'
      node-version: '21'
      slack-channel: '#frontendstøtte-ci'
      slack-bot-name: 'Biome'
      slack-bot-icon: ':mag:'

