name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@3b34a5629a335f326b4007a909a8c5e729367b00
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      node-version: '21'
      slack-channel: '#frontendstøtte-ci'
      slack-bot-name: 'Biome'
      slack-bot-icon: ':mag:'

