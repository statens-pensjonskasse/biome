name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@2-etablere-npmfrontend-workflow
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      node-version: '22'
      slack-channel: '#github-alerts'
      slack-bot-name: 'GitHub Actions'
      slack-bot-icon: ':monkey:'

