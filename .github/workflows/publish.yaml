name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@feature/build-app-node-workflow
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      node-version: '22'
      slack-channel: '#github-alerts'
      slack-bot-name: 'Tofik Test Node Library'
      slack-bot-icon: ':monkey:'

