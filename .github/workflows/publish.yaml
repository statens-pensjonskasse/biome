name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@ff884733c06045d6b33f52a62cd44db3dbd2f6ee
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      test-goals: 'run lint'
      node-version: '21'
      slack-channel: '#frontendstøtte-ci'
      slack-bot-name: 'Biome'
      slack-bot-icon: ':mag:'

