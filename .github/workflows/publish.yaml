name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/github-actions-library/.github/workflows/build-library-npm-yarn.yaml@4f31d648e1345e52359b683e1d6d24328616d441
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      test-goals: 'run lint'
      node-version: '21'
      slack-channel: '#frontendstøtte-ci'
      slack-bot-name: 'Biome'
      slack-bot-icon: ':mag:'

