name: Build and Publish to GitHub Packages

on:
  push:
  workflow_dispatch:

jobs:
  build:
    uses: statens-pensjonskasse/public-actions-library/.github/workflows/build-library-node.yaml@04b1606a939935f7ef9b875107cfdd4776549dcb # v0.2.8
    permissions:
      contents: write
      packages: write
    secrets: inherit
    with:
      node-version: '21'
